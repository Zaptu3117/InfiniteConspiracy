{
  "name": "infinite-conspiracy-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Infinite Conspiracy detective game",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:kusama": "hardhat run scripts/deploy.js --network kusama",
    "verify": "hardhat run scripts/verify.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}

